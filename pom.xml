<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>io.github.luciferyang</groupId>
  <artifactId>cached-fs-parent</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <packaging>pom</packaging>

  <name>cached-fs (parent)</name>
  <description>
    Java reimplementation of the velox file read cache: a per-process,
    two-tier (RAM + local SSD) read cache exposed as a transparent
    Hadoop FileSystem decorator.
  </description>
  <url>https://github.com/luciferyang/cached-fs</url>

  <licenses>
    <license>
      <name>The Apache License, Version 2.0</name>
      <url>https://www.apache.org/licenses/LICENSE-2.0.txt</url>
      <distribution>repo</distribution>
    </license>
  </licenses>

  <modules>
    <module>cached-fs-core</module>
    <module>cached-fs-hadoop</module>
    <module>cached-fs-spark</module>
    <module>cached-fs-metrics</module>
    <module>cached-fs-cli</module>
  </modules>

  <properties>
    <maven.compiler.release>21</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

    <hadoop.version>3.4.1</hadoop.version>
    <slf4j.version>2.0.16</slf4j.version>
    <junit.version>5.11.3</junit.version>
    <assertj.version>3.26.3</assertj.version>
    <mockito.version>5.14.2</mockito.version>
    <jcstress.version>0.16</jcstress.version>
    <micrometer.version>1.14.1</micrometer.version>
    <picocli.version>4.7.6</picocli.version>

    <s3mock.version>4.11.0</s3mock.version>
    <!-- s3mock pulls in jackson 2.19.x; hadoop pulls in 2.12.x. The combination NoClassDefs on
         JacksonFeature unless we align. Pinning to 2.19.4 works because Jackson 2 is wire- and
         API-compatible across the 2.10–2.19 minor releases that Hadoop touches. -->
    <jackson.version>2.19.4</jackson.version>

    <spotless.version>2.43.0</spotless.version>
    <surefire.version>3.5.2</surefire.version>
    <jacoco.version>0.8.12</jacoco.version>
    <maven-compiler-plugin.version>3.13.0</maven-compiler-plugin.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>com.fasterxml.jackson</groupId>
        <artifactId>jackson-bom</artifactId>
        <version>${jackson.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>io.github.luciferyang</groupId>
        <artifactId>cached-fs-core</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>io.github.luciferyang</groupId>
        <artifactId>cached-fs-hadoop</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>io.github.luciferyang</groupId>
        <artifactId>cached-fs-metrics</artifactId>
        <version>${project.version}</version>
      </dependency>

      <dependency>
        <groupId>org.slf4j</groupId>
        <artifactId>slf4j-api</artifactId>
        <version>${slf4j.version}</version>
      </dependency>
      <dependency>
        <groupId>org.apache.hadoop</groupId>
        <artifactId>hadoop-common</artifactId>
        <version>${hadoop.version}</version>
        <scope>provided</scope>
      </dependency>

      <dependency>
        <groupId>org.junit</groupId>
        <artifactId>junit-bom</artifactId>
        <version>${junit.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>org.assertj</groupId>
        <artifactId>assertj-core</artifactId>
        <version>${assertj.version}</version>
        <scope>test</scope>
      </dependency>
      <dependency>
        <groupId>org.mockito</groupId>
        <artifactId>mockito-core</artifactId>
        <version>${mockito.version}</version>
        <scope>test</scope>
      </dependency>
      <!-- Bytecode-attachment agent for Mockito's inline mock-maker (mocking final classes).
           Pinned explicitly so JDK 24+ — which removed dynamic agent self-attach — does NOT fail
           with 'Could not initialize plugin: MockMaker'. Wired into surefire via -javaagent below. -->
      <dependency>
        <groupId>net.bytebuddy</groupId>
        <artifactId>byte-buddy-agent</artifactId>
        <version>1.15.4</version>
        <scope>test</scope>
      </dependency>
      <dependency>
        <groupId>org.openjdk.jcstress</groupId>
        <artifactId>jcstress-core</artifactId>
        <version>${jcstress.version}</version>
        <scope>test</scope>
      </dependency>
      <dependency>
        <groupId>org.apache.hadoop</groupId>
        <artifactId>hadoop-minicluster</artifactId>
        <version>${hadoop.version}</version>
        <scope>test</scope>
      </dependency>
      <dependency>
        <groupId>org.apache.hadoop</groupId>
        <artifactId>hadoop-aws</artifactId>
        <version>${hadoop.version}</version>
        <scope>test</scope>
      </dependency>
      <dependency>
        <groupId>com.adobe.testing</groupId>
        <artifactId>s3mock-junit5</artifactId>
        <version>${s3mock.version}</version>
        <scope>test</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <!-- Pinned for reproducibility: the release=21 compile flag relies on a recent compiler
               plugin; without a pin the version resolves from the active Maven distribution's
               super-POM, which drifts across Maven 3.8/3.9/4.x. -->
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>${maven-compiler-plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-surefire-plugin</artifactId>
          <version>${surefire.version}</version>
          <configuration>
            <!-- @{argLine} expands JaCoCo's late-bound property so coverage works alongside our flags.
                 -javaagent attaches byte-buddy-agent so Mockito's inline mock-maker can rewrite
                 final classes on JDK 24+, which removed dynamic agent self-attach.
                 -Djava.security.manager=allow restores the legacy Subject.getSubject implementation
                 on JDK 24+, which Hadoop UGI (called by Spark's SparkContext init) still needs. -->
            <argLine>@{argLine} -javaagent:${net.bytebuddy:byte-buddy-agent:jar} --enable-native-access=ALL-UNNAMED -Djava.security.manager=allow</argLine>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-failsafe-plugin</artifactId>
          <version>${surefire.version}</version>
          <configuration>
            <!-- @{argLine} expands JaCoCo's late-bound property so coverage works alongside our flags.
                 -javaagent attaches byte-buddy-agent so Mockito's inline mock-maker can rewrite
                 final classes on JDK 24+, which removed dynamic agent self-attach.
                 -Djava.security.manager=allow restores the legacy Subject.getSubject implementation
                 on JDK 24+, which Hadoop UGI (called by Spark's SparkContext init) still needs. -->
            <argLine>@{argLine} -javaagent:${net.bytebuddy:byte-buddy-agent:jar} --enable-native-access=ALL-UNNAMED -Djava.security.manager=allow</argLine>
          </configuration>
          <executions>
            <execution>
              <goals>
                <goal>integration-test</goal>
                <goal>verify</goal>
              </goals>
            </execution>
          </executions>
        </plugin>
        <plugin>
          <groupId>com.diffplug.spotless</groupId>
          <artifactId>spotless-maven-plugin</artifactId>
          <version>${spotless.version}</version>
          <configuration>
            <java>
              <googleJavaFormat>
                <version>1.24.0</version>
                <style>GOOGLE</style>
              </googleJavaFormat>
              <removeUnusedImports/>
              <licenseHeader>
                <file>${maven.multiModuleProjectDirectory}/build/license-header.txt</file>
              </licenseHeader>
            </java>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.jacoco</groupId>
          <artifactId>jacoco-maven-plugin</artifactId>
          <version>${jacoco.version}</version>
          <executions>
            <execution>
              <id>prepare-agent</id>
              <goals><goal>prepare-agent</goal></goals>
            </execution>
            <execution>
              <id>report</id>
              <phase>verify</phase>
              <goals><goal>report</goal></goals>
            </execution>
            <!-- Failsafe runs in a separate forked JVM from Surefire; without a dedicated
                 prepare-agent-integration execution, IT coverage is silently dropped. -->
            <execution>
              <id>prepare-agent-integration</id>
              <goals><goal>prepare-agent-integration</goal></goals>
            </execution>
            <execution>
              <id>report-integration</id>
              <phase>verify</phase>
              <goals><goal>report-integration</goal></goals>
            </execution>
          </executions>
        </plugin>
      </plugins>
    </pluginManagement>

    <plugins>
      <!-- Exposes each project dependency's local JAR path as a property like
           ${net.bytebuddy:byte-buddy-agent:jar} so surefire's argLine can attach
           byte-buddy-agent as a JVM -javaagent. -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-dependency-plugin</artifactId>
        <executions>
          <execution>
            <id>resolve-dependency-properties</id>
            <phase>initialize</phase>
            <goals>
              <goal>properties</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-failsafe-plugin</artifactId>
      </plugin>
      <plugin>
        <groupId>org.jacoco</groupId>
        <artifactId>jacoco-maven-plugin</artifactId>
      </plugin>
      <plugin>
        <groupId>com.diffplug.spotless</groupId>
        <artifactId>spotless-maven-plugin</artifactId>
        <executions>
          <execution>
            <id>spotless-check</id>
            <phase>verify</phase>
            <goals><goal>check</goal></goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
